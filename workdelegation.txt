SAM

Knowledge Graph Engine + Visualization
This person owns the data backbone everything else plugs into. build the KG Builder (LLM parses course material → extracts concepts + prerequisites → NetworkX directed graph), define the node data model (mastery score, status, careless badge, decay timestamp, attempt/correct counts), and build the Pyvis/Streamlit graph renderer with color-coded nodes and prerequisite edge highlighting. wire in real-time visual updates (node color shifts on mastery change, opacity fade for decay, CSS pulse on newly discovered gaps, prerequisite chain highlighting when a conceptual gap is found), and handle the "chain turns green" effect when a root prerequisite is fixed. This person delivers the API: add_concept(), update_mastery(), get_prerequisites(), get_dependents(), render_graph(). Everything else calls these.

CHAI YIN 
 Assessment + Quiz + Classification
This person owns the student-facing question flow. build the quiz generator (LLM creates questions per concept from the knowledge graph), confidence rating UI (1-5 before seeing results), answer evaluation, and the critical LLM classification step — prompt engineering to distinguish careless vs conceptual mistakes and extract the exact error and missing concept. Build the self-awareness score tracker (running accuracy of confidence vs actual performance). build micro-checkpoints (single verification question after each intervention), the human override button ("I understand this, I just rushed" → reclassify from conceptual to careless), and blind spot found/resolved counter. This person delivers: generate_quiz(concept), evaluate_answer(), classify_mistake(), get_self_awareness_score().


JOASH
 BKT + RPKT + Planner (Core Algorithms)
This person owns the intelligence layer — the algorithms that make the system actually adaptive.  implement the BKT engine (per-concept mastery tracking with Bayesian updates on each interaction), temporal decay model (exponential forgetting curve, configurable decay rate), and the RPKT module (recursive prerequisite probing — when a conceptual gap is found, walk backward through the graph probing with diagnostic questions until you find the knowledge boundary). build the time-constrained planner — the scoring formula (gap_severity × prereq_depth × decay_risk × careless_frequency), greedy time-budget filling, and the mission briefing output. Also build the peer hub matching algorithm (4-tier classification, complementarity scoring across concept profiles, balanced group formation). This person delivers: update_bkt(), get_mastery(), apply_decay(), run_rpkt_probe(), generate_study_plan(minutes), match_hubs(students).

YICHEN
 RAG + Socratic Tutor + Interventions
This person owns what the student actually interacts with during learning.  build the RAG pipeline (embed course content chunks into ChromaDB with concept_id tags, retrieval function that pulls relevant content for a given concept), and the Socratic tutor (LLM chat that receives current knowledge state, identified gaps, and RAG context — never gives direct answers, only guiding questions). implement the two intervention paths — careless path (re-present the question with a pattern warning) and conceptual path (prerequisite backtrack using RPKT results → Socratic micro-lesson → scaffolded questions building back up to the failed topic). Also build the session summary generator (aggregate session stats + LLM generates "biggest win" highlight and learning velocity comparison). This person delivers: embed_content(), retrieve_context(concept), tutor_chat(message, knowledge_state), run_intervention(mistake_type), generate_session_summary().



SEANN

Frontend + Dashboard + Integration + Deliverables
This person owns the app shell, the dashboard, and making sure everything works together. set up the Streamlit app structure (or Next.js if the team prefers), build the main navigation and page layouts, create the quiz-taking UI (question display, confidence slider, answer input, result screen with classification), and the "I have X minutes" input + mission briefing display. build the dashboard (mastery-over-time chart with Plotly, AI insights panel, careless vs conceptual breakdown, peer hub matching visualization with the complementary profile display), the session summary screen, and — critically — wire all four other people's backends together into the working app. This person also owns the deliverables: generating synthetic demo data (8 students, 2 weeks of history), writing the README, setting up testbench/ with TESTING.md and quick_start.sh, recording the demo video, and assembling the documentation PDF.